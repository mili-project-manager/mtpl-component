module.exports = {
  files: ['tests/**/*.ts', '!tests/**/*.before-each.ts'],
  typescript: {
    rewritePaths: {
      'tests/': 'lib/tests/',
    },
    compile: false,
  },
{{#if (equals answers.module "esnext")}}
  nodeArguments: [
    '--loader=ts-node/esm',
  ],
{{/if}}
}
